name: Terraform View Remote State Without Code

on:
  workflow_dispatch:  # run manually

jobs:
  view-state:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
    - name: Checkout (empty repo)
      uses: actions/checkout@v4

    - name: "Auth in GCP"
      id: "auth"
      uses: "google-github-actions/auth@v2"
      with:
        create_credentials_file: true
        workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER_ID }}
        service_account: ${{ secrets.GCP_WORKLOAD_IDENTITY_SA_EMAIL }}
        project_id: 'p-601-np-membership-adt'

    - name: Install Terraform
      uses: hashicorp/setup-terraform@v3

    - name: Create minimal Terraform backend config
      run: |
        cat > main.tf <<EOF
        terraform {
          backend "gcs" {
            bucket = "gcp-gcs-cicd-core-mbr-bc-lead"
            prefix = "adt/bc"
          }
        }
        EOF

    - name: Initialize Terraform backend
      run: terraform init -input=false

    - name: Disable deletion protection via gcloud
      run: |
        gcloud sql instances patch lead-mgmt-adt \
          --no-deletion-protection \
          --project=p-601-np-membership-adt

    - name: List resources in remote state
      run: terraform state list

    - name: Refresh Terraform state
      run: terraform refresh -input=false

    - name: Remove deletion protection and destroy instance
      run: |
        cat > main.tf <<EOF
        terraform {
          backend "gcs" {
            bucket = "gcp-gcs-cicd-core-mbr-bc-lead"
            prefix = "adt/bc"
          }
        }

        resource "google_sql_database_instance" "this" {
          name                 = "lead-mgmt-adt"
          deletion_protection  = false
          project              = "p-601-np-membership-adt"
          region               = "us-central1"
          settings {
            tier = "db-f1-micro"
          }
        }
        EOF

        terraform init -input=false

        terraform import google_sql_database_instance.this p-601-np-membership-adt:<instance-name>

        terraform apply -auto-approve

        terraform destroy -auto-approve -target=google_sql_database_instance.this